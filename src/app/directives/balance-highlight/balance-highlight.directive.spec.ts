import { BalanceHighlightDirective } from './balance-highlight.directive';

describe('BalanceHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BalanceHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
