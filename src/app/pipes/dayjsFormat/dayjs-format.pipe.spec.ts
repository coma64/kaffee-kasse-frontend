import { DayjsFormatPipe } from './dayjs-format.pipe';

describe('DayjsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DayjsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
